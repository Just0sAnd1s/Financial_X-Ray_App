# This file lists the Python libraries needed to run the app.
# By not pinning versions, we allow Streamlit to find the
# latest compatible versions for its Python environment.

streamlit
pandas
yfinance
